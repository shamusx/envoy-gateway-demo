apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: httpbin-wc
  namespace: httpbin
spec:
  secretName: wildcard-cert
  issuerRef:
    name: selfsigned-ca
    kind: ClusterIssuer
  commonName: "*.httpbin.io"
  dnsNames:
  - "*.httpbin.io"
  - "*.httpbin-internal.io"